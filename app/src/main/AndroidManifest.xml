<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.aulabancosqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_dente"
        android:label="Smile Care"
        android:roundIcon="@mipmap/ic_launcher_dente_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cadastrese"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".menu"></activity>
        <activity android:name=".meusdados"></activity>
        <activity android:name=".ConsultaLista"></activity>
        <activity android:name=".pedido"></activity>
        <activity android:name=".agendamento"></activity>
    </application>

</manifest>